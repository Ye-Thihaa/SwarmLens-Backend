fastapi==0.115.6
uvicorn[standard]==0.34.0
aiosqlite==0.20.0
httpx==0.28.1
# pydantic-core only publishes a Windows cp314 (Python 3.14) wheel from
# 2.40.0 onward -- 2.27.0 and 2.33.0 confirmed do NOT have one. Below
# that floor, pip falls back to compiling pydantic-core from source
# (Rust + the MSVC linker), which fails on a machine without Visual
# Studio Build Tools installed. The explicit pydantic-core floor (not
# just a pydantic floor) is what actually pins the fix -- pydantic's own
# version number doesn't move in lockstep with pydantic-core's.
pydantic>=2.11,<3
pydantic-core>=2.40

# ai_engine.py -- saliency (contrib, not base opencv-python, for cv2.saliency),
# CLIP inference via ONNX Runtime, CLIP's tokenizer. See ai_engine.py's module
# docstring for exact pretrained checkpoints used (downloaded on first use).
# headless, not the GUI build -- this only ever calls cv2.saliency/threshold/
# cvtColor (no imshow/highgui), and the GUI build's libGL/libxcb/... deps
# aren't present on python:3.12-slim (confirmed: `import cv2` in the
# container raised ImportError: libxcb.so.1: cannot open shared object file).
opencv-contrib-python-headless==5.0.0.93
numpy==2.5.1
pillow==12.3.0
onnxruntime==1.28.0
tokenizers==0.23.1

# Face mesh for eyes-open / smiling (ai_engine.face_expression). Its
# blendshapes are trained outputs, so the thresholds are the model's own
# calibration rather than numbers fitted to one photo -- which is what
# YuNet's 5 landmarks could never support (one point per eye, its centre,
# identical whether the eye is open or shut). The 1.0.0 wheel is
# py3-none-win_amd64, so it installs on 3.14 unlike many native wheels.
mediapipe>=1.0.0
