// Matches the example zones used throughout README.md's demo commands.
// The "real" guest UI (item #4 of the project's task list) has its own
// actual venue zone list; this scaffold isn't wired to that, so these
// are just plausible stand-ins for exercising the capture/outbox flow.
export const ZONES = ["flower_arch", "bar", "dance_floor", "photo_booth", "entrance"];
